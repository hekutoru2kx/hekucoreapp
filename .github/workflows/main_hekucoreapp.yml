# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy ASP.Net Core app to Azure Web App - hekucoreapp

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: read #This is required for actions/checkout

    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.x'

      - name: Install Angular dependencies
        run: npm ci
        working-directory: ./frontend

      - name: Build Angular
        run: npm run build
        working-directory: ./frontend

      - name: Copy Angular build to wwwroot
        # wwwroot is .gitignored; this rebuilt bundle is the single source of the deployed
        # frontend, picked up by `dotnet publish ./backend/Hekucoreapp.Api` below.
        run: |
          New-Item -ItemType Directory -Force -Path ./backend/Hekucoreapp.Api/wwwroot
          Copy-Item -Recurse -Force ./frontend/dist/frontend/browser/* ./backend/Hekucoreapp.Api/wwwroot/

      - name: Set up .NET Core
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.x'

      - name: Build with dotnet
        run: dotnet build ./backend --configuration Release

      - name: dotnet publish
        run: dotnet publish ./backend/Hekucoreapp.Api -c Release -o "${{env.DOTNET_ROOT}}/myapp"

      - name: Upload artifact for deployment job
        if: github.event_name != 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: .net-app
          path: ${{env.DOTNET_ROOT}}/myapp

  deploy:
    runs-on: windows-latest
    needs: build
    if: github.event_name != 'pull_request'
    permissions:
      id-token: write #This is required for requesting the JWT
      contents: read #This is required for actions/checkout

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: .net-app

      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_13A547E87D2A4C3FAB5753C611413CB6 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_13B295BF33AC4B849477524C6CFF57DB }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_9B3CF1E202474536A9B008AEFB455C45 }}

      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'hekucoreapp'
          slot-name: 'Production'
          package: .
