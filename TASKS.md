# Tasks

Single source of truth for this project's tasks. Structure is shared across the whole
family (ludemia, hekucoreapp, hekutenantcoreapp, gestamind): **Pending** = queued work,
**Deferred** = intentionally postponed with a stated trigger, **Done** = shipped.

## Pending

- [ ] **Bug: `ContentService.UploadToSlotAsync` crashes with a raw ImageSharp exception on a wrong-file-type upload instead of a clean rejection** — found 2026-09-12 while building ludemia's own, separate `CourseContentRepository.UploadFileAsync` (which mirrors this method's pattern for images without calling it). The existing code assumes `Image.DetectFormatAsync` returns `null` for data it can't identify (`if (detectedFormat == null || !allowedTypes.Contains(...)) throw ...`), then only calls `Image.LoadAsync` inside the "recognized" branch. In practice, testing against real non-image uploads (plain text, a PDF) in ImageSharp 3.1.12 showed `Image.LoadAsync` throws `UnknownImageFormatException` — with a raw multi-line "Image cannot be loaded. Available decoders: ..." message — reaching this path anyway, meaning `PersonController`'s/`UserController`'s profile-picture upload almost certainly returns that raw exception text as a 400 body today instead of the intended localized `ContentFileTypeNotAllowed` message, for any wrong-type upload attempt. Likely never caught because the feature's original verification only smoke-tested the happy path + a 401 check, not a wrong-type negative case. **Fix applied in ludemia's own (separate) copy**: attempt `Image.LoadAsync` directly inside a try/catch and use success/failure as the "is this an image" signal (reading the format back off `image.Metadata.DecodedImageFormat` on success) rather than a `DetectFormatAsync` null-check — see `CourseContentRepository.UploadFileAsync`. The same fix has **not** been applied here yet. _Trigger: next time this file is touched, or before relying on that error path for a real user-facing feature._

## Deferred — intentional, revisit on trigger

- [ ] **Signed-license-file tech enforcement for the core** — Héctor decided (2026-09-02) to add software licensing / entitlement enforcement to the hekucoreapp core, postponed until the core has a complete feature set. Technical design is agreed (don't re-litigate): offline signed license file (`{ payload, signature, alg:"RS256", kid }`), private `LicenseGen` CLI holds the RSA-2048 key, in-app verifier ships only the embedded public key; `LicenseClaims` record in Domain, `ILicenseService`/`FileLicenseService` singleton in Infrastructure verifying once at startup; `LicenseEnforcementMiddleware` beside `ActiveUserMiddleware` (402 on Missing/Invalid/Expired, `X-License-Warning` on Grace), module gating via the `PermissionCatalog.Modules` authorization loop, seat check in `UserManagementService`, `GET /api/license` for the frontend; ship disabled/log-only until deliberately flipped on. Full design: `d--hekucoreapp` memory `project_licensing_enforcement_todo`. Legal license model deliberately left open. _Trigger: the core stabilises / a product built on it needs a monetization path._

## Done

- [x] **Generic content/attachments layer + Person profile picture (2026-09-11)** — standardized ludemia's ad-hoc `CourseContent` feature (single-entity, hard-coded to Course/ClassSession) into a reusable capability any entity can opt into, following the family's existing `LocalizedText` polymorphic-side-table pattern (which hekucoreapp lacked). New generic `ContentItem` entity (`content_items`: `OwnerType`/`OwnerId` — deliberately no FK, any future entity can be an owner — plus `Slot` for a singleton item like an avatar vs. `null` for an ordered list; `Kind` enum Text/Link/Video/Document/Image/File, only Image/File wired to an endpoint so far) and `StoredFile` (blob metadata — container/blob name/content type/size/sha256/dimensions; bytes never touch Postgres). `IContentStorage` abstraction with two implementations selected by `ContentStorage:Provider` config: `LocalDiskContentStorage` (default, writes under `App_Data/uploads`, needs no Azure account) and `AzureBlobContentStorage` (`Azure.Storage.Blobs`, connection-string auth). Uploads go through `ContentService.UploadToSlotAsync`: buffers and sniffs the real format (never trusts the client content-type), validates size/type against **admin-tunable `AppSettings` fields** (`ContentMaxBytes`, `ContentAllowedContentTypes`, `ContentMaxImageDimension`, `ContentAvatarMaxDimension` — clamped to a `ContentStorage:HardMaxBytes` ceiling in `appsettings.json`), resizes+re-encodes with `SixLabors.ImageSharp` (strips EXIF, bounds stored size), stores the blob, then hard-deletes any previously replaced blob. Generic mechanics only — **no generic write controller**: each owning feature exposes its own endpoints under its own route and enforces its own existing permissions before delegating (mirrors `LocalizedText` having no controller of its own). The one shared endpoint is `GET /api/content/{id}/file`, which delegates its read-authorization check to whichever `IContentAccessPolicy` is registered for that item's `OwnerType` (`PersonContentAccessPolicy`: the caller's own person, or anyone with `PersonsPermission.Read`) — an owner type with no registered policy is denied by default. First (and so far only) consumer: **Person profile picture**, a `Slot = "ProfilePicture"` singleton wired self-service on `UserController` (`PUT/DELETE api/user/person/profile-picture`, resolves the caller's own `PersonId`) with a new reusable `app-avatar-upload` component replacing the static `account_circle` icon on the profile page. An equivalent admin-side endpoint on `PersonController` (`PersonsPermission.Update`) is deliberately not wired yet — same service, just not called from there. Design doc (generic-vs-per-entity trade-offs, the multi-tenant blob-partitioning plan for the tenant cores, deferred fork ports and ludemia's eventual `CourseContent` migration onto this) is `d--hekucoreapp` memory `project_content_standardization`. **Verified:** `dotnet build` clean; migration `AddContentItems` applied to the live dev DB (explicit `HasDefaultValue` on the four new `AppSettings` columns so the pre-existing singleton row backfills to real defaults, not CLR zeros — an empty `ContentAllowedContentTypes` would have silently rejected every upload); app boots clean against the live DB (`ValidateOnBuild` in Development also confirms the new DI graph resolves); `GET/PUT` smoke-tested unauthenticated → 401 as expected. `ng build` clean (pre-existing bundle-budget warning unchanged); `ng test` shows the same ~12 pre-existing "should create" smoke-test failures as every other page (missing TestBed providers, unrelated to this change) — no regressions. Not exercised through the browser with a real login. Fork ports (hekutenantcoreapp/gestamind — add `tenant_id` + `Tenant.StoragePrefix` blob partitioning; ludemia — migrate `course_contents` onto this) are follow-up work, not done here. **Deployed to the Azure App Service same day** with `ContentStorage` repointed at a real `hekustorage` blob container (`ContentStorage__Provider=AzureBlob`) — real browser testing there caught two bugs, fixed same day: (1) `UserController`'s `PersonNotFound` guard passed a `LocalizedString` (not `.Value`) into `BadRequest(object)`, which JSON-serializes as an object — the frontend rendered literal `"[object Object]"` instead of the message (hit by the bootstrap admin account, which had no `Person` record yet — profile-picture upload requires one); `avatar-upload.ts` also hardened to only render `err.error` when it's actually a string. (2) `Program.cs` was missing `app.MapFallbackToFile("index.html")` (present in hekutenantcoreapp/gestamind, never ported here) — any hard refresh or direct link to a client-side route (`/profile`, `/admin/roles`, …) 404'd. Fix verified locally by copying a built frontend into `wwwroot` (can't otherwise exercise the fallback in dev, where Angular runs via `ng serve`): deep links now 200. (3) The download endpoint requires a bearer token, but a plain `<img src>` never carries one (browsers only attach one via `HttpClient`'s interceptor) — the picture "uploaded" but never rendered, and opening its URL directly 401'd. `AvatarUpload` now takes a `contentId` and fetches the file itself via a new `Content.fetchImage()` (`HttpClient`, `responseType: 'blob'`), binding the result as an object URL instead of the raw API URL — fixes "open in new tab" for free too, since that now opens the already-fetched blob. **Confirmed working end-to-end on Azure after this fix.** Also, while scoping the hekutenantcoreapp/gestamind port, `IContentStorage.SaveAsync` was simplified to resolve its own storage partition internally (via `IContentPartitionResolver`, moved to `Infrastructure.Content`) rather than take one as a parameter — `ContentService` (Application layer) has no clean way to resolve "the current tenant" itself, only Infrastructure can. Behavior unchanged here (still "global"); this is what let the multi-tenant port's resolver hook the DbContext directly. All re-verified: `dotnet build` clean, app boots clean.
- [x] **CSV reference-data seeder + geography seed data (2026-09-10)** — a fresh/recreated DB had no way back to the ~153k-row geography reference set (dr5hn Countries-States-Cities, ODbL v1.0); it had only ever been hand-imported, and the 2026-09-09 core DB recreations lost it (both cores sat at `countries/states/cities = 0`). New generic `CsvSeeder.SeedAsync(DbContext, folder, params (table, columns)[])` in `Infrastructure/Data` — for each named table that is **currently empty**, decompresses `Resources/<folder>/<table>.csv.gz` and bulk-loads it via PostgreSQL `COPY … FROM STDIN (FORMAT csv)` (header row skipped, body streamed raw so quoted newlines survive), then `setval(pg_get_serial_sequence(...))` past the seeded ids (the `id` cols are `GENERATED BY DEFAULT`). No-op once populated. `columns` is a developer constant spliced into COPY — same trust level as migration SQL. `GeographySeeder.SeedAsync(db)` is the thin caller (countries → states → cities, FK order); it's invoked in `Program.cs` startup right after `AppSettingsSeeder`. Resources: `backend/Hekucoreapp.Infrastructure/Resources/Geography/{countries,states,cities}.csv.gz` (exported from ludemia's live DB — the family's source of truth — via `COPY … TO STDOUT`, gzipped; **1.4 MB total**, cities 1.31 MB), shipped through the csproj `<None … CopyToOutputDirectory="PreserveNewest">`. **Pattern is reusable** for any future project-DB reference data (e.g. a ludemia course-subject catalog): add a `.csv.gz` + a small named seeder. **Verified end-to-end:** dropped all 15 `public` tables → `dotnet ef database update` → started the app → `GeographySeeder` loaded **250 / 5,308 / 152,967** (exact match to gestamind/ludemia), `GET /api/geography/countries` returns data, and the same fresh-start pass created the bootstrap admin (below) — `POST /api/auth/login` with `Admin@1234` → 200 + JWT with full admin claims, `mustChangePassword:true`. Re-boot with tables populated: seeder no-ops, clean startup.
- [x] **Auto-create the bootstrap admin on a fresh/empty database (2026-09-10)** — provisioning a new DB used to be a two-step manual dance: self-register through the running app *first*, then restart so the `Program.cs` startup block promotes that now-existing user. Startup only ever *granted a role to a user that already existed* — never created one — so a fresh DB had no first way in, and "I set `BootstrapAdminEmail` but can't log in" was a predictable footgun (hit again in the 2026-09-09 DB-recreation session). Now, in the startup scope, when `BootstrapAdminEmail` is set **and `AspNetUsers` is entirely empty** (`!await userManager.Users.AnyAsync()` — guard on *empty table*, never merely "no active admin", so it can never fire on an established install), the block creates that user (`EmailConfirmed = true`, `MustChangePassword = true`, `IsActive = true`, `UserName = email`) and the existing promotion path picks it up in the **same** pass — hekucoreapp grants `Admin`; hekutenantcoreapp grants `SuperAdmin` + default-tenant membership + tenant-scoped `Admin`. Password: new optional `BootstrapAdminPassword` config key (blank default in `appsettings.json`, set via user-secrets) — if unset, a random 20-char CSPRNG password (`RandomNumberGenerator.GetString` over an unambiguous alphabet + `Aa1!` to clear the Identity complexity rules) is generated and logged **once** at `Warning`. `userManager.CreateAsync` failure logs at `Warning` and falls through — startup never crashes on it. hekucoreapp block sits right after `EnsureDefaultRolesExistAsync()`; hekutenantcoreapp block sits just before the existing `bootstrapUser` lookup so the create flows straight into the SuperAdmin/default-tenant wiring. No migration (Identity data, not schema). **Verified:** `dotnet build` clean in both cores (pre-existing `UserRepository.cs` CS8619 warning unchanged); hekutenantcoreapp `dotnet test` 54/54 green; both APIs boot cleanly against their live DBs — the new `AnyAsync` guard runs and correctly skips the create block (both DBs already have users), so the happy path (empty DB) is reasoned-through, not browser-verified.
- [x] **`DataTableColumn.header` made optional + ludemia's thin client tables folded in (2026-09-09)** — follow-up to the data-table port below. `shared/data-table-controller.ts`: `header` is now `?: () => string`, falling back to the column `key` (a private `headerLabel()` resolves it in the two computeds that read it — `reorderableColumns` and `exportColumns`). A client-mode table that shows no column menu and no CSV export can now declare columns as just `{ key, sortable?, sortValue? }`. Change re-synced **byte-identical** to hekutenantcoreapp / gestamind / ludemia (spec gets a 13th test + the prefix swap). With that unblocked, ludemia's 5 remaining `MatTableDataSource`-only tables were migrated: `my-invoices`, `my-enrollments` (pure client — sort + client paginate), `membership-oversight`, `membership-plan-management` (client sort, no paginator), and `my-billing` (client sort of a **server-paged** page — `attachSort` only, its own `mat-paginator`/`onPageChange` kept). Each dropped its hand-rolled `sortingDataAccessor` switch + `@ViewChild` wiring for `table = new DataTableController({ mode: 'client', columns })` + `attachSort`/`attachPaginator` + an `effect(() => table.setData(...))`. **Verified:** `ng build` + the 13 controller tests green in all four repos, no regressions. The family-wide data-table port is now complete with nothing outstanding.
- [x] **Reusable sortable data-table — `DataTableController<T>` (2026-09-09)** — every admin grid hand-copied ~120 lines of the same glue (column-order signal, hidden-columns `Set`, `restore/persistColumnPreferences`, `onColumnsReordered`/`onColumnVisibilityToggled`, the `reorderableColumns` projection, a `columnDefs()` map, `getExportColumns`, `onSortChange`/`onPageChange`, the `displayedColumns` computed) and they had drifted (`user-management` persisted only order+hidden; `person-management` had no column menu or export at all). New `frontend/src/app/shared/data-table-controller.ts` — a framework-free class (only `signal`/`computed`, no injection context) instantiated as a component field `table = new DataTableController<Row>({ tableKey, columns, defaultSort, onChange, mode })`. It owns column order/visibility/sort/pagination + `localStorage` prefs (via the existing `column-preferences.ts`) + the `<app-column-reorder>` and CSV-export (`csv-export.ts`) projections; the page keeps its own `<table mat-table>` markup, cell templates and actions column. **Two data modes**: `server` (sort/page → `onChange` reload, the default) and `client` (owns a `MatTableDataSource`, `setData()` + `attachSort`/`attachPaginator` from `@ViewChild` setters). Column config: `{ key, header: () => string, sortable?, sortKey?, hideable?, hidden?, exportValue?, sortValue? }`. Migrated all three fitting admin tables: **user-management** (server, straight refactor + gains the initial `[matSortActive]` arrow), **person-management** (server, *gains* the column menu + CSV export + drag-reorder it lacked), **role-management** (converted to **client** mode — `MatTableDataSource` + `MatSort` + `MatPaginator`, `name`/`claims` now sortable — the in-repo consumer of the client path). `user-role-assignment` left alone (per-row datepicker editor, not a data grid). Net −70 lines across the 3 pages, and every future table wires ~15 lines of column config instead of ~120. New `data-table-controller.spec.ts` (12 pure-class tests, all green). **Verified:** `ng build` clean (pre-existing bundle-budget warning unchanged); `ng test` — the 12 new tests pass, no regressions (the pre-existing component smoke-test failures are missing-provider gaps on `main` too). Not browser-verified — hekucoreapp's Supabase DB is unreachable (project reaped). Forks not touched — see the Pending port item.
- [x] **Gitignored `wwwroot`; Azure deploy workflow now builds the Angular frontend (2026-09-09)** — an Azure-generated deploy workflow (`main_hekucoreapp.yml`) was added this day; it built/published only the repo root with no frontend step and a pathless `dotnet build` that wouldn't have found `backend/Hekucoreapp.slnx`. hekucoreapp also had no `wwwroot` directory at all. Now `wwwroot/` is `.gitignored` with a committed `.gitkeep` (so `.NET`'s static-files middleware finds it at startup), and the workflow gained Node setup + `npm ci` + `npm run build` + "Copy Angular build to wwwroot" + `dotnet build ./backend` / `dotnet publish ./backend/Hekucoreapp.Api`; also runs on `pull_request` (build only — no test project). `angular.json` was already building to `frontend/dist/frontend/browser`. Family-wide standardization — see ludemia / gestamind / hekutenantcoreapp.
- [x] **Ported `EnsureDefaultRolesExistAsync` startup role-seeding from ludemia (2026-09-09)** — `DefaultRoleCatalog.Roles` was read only by `RoleManagementRepository.RestoreDefaultRolesAsync()` (reachable only via `POST api/admin/roles/restore-defaults`); the `Program.cs` startup block seeded only `Admin`, so on a fresh database any grant of a catalog role (`UserManagementRole`, `PersonManagementRole`) threw `RoleNotFound` until an admin pressed that button. Added `IRoleManagementRepository.EnsureDefaultRolesExistAsync()` — creates only the *missing* catalog roles with their catalog claims, touching nothing that already exists — and call it from the `Program.cs` startup scope right after the `Admin` role is ensured. Deliberately not `RestoreDefaultRolesAsync()` (full reconcile, removes stray claims, would undo admin customisations every restart). hekucoreapp's `DefaultRoleCatalog` has no `ClaimlessRoles` (a ludemia-only concept), so the port is just the `Roles` loop. Build clean; no migration (Identity data, not schema). This was the last outstanding ludemia→core port.
- [x] **Validate email during registration (2026-09-09)** — added an admin-configurable **"require email confirmation"** toggle plus the full verify flow. New generic `AppSettings` singleton infrastructure (entity `app_settings` Id=1, `AppSettingsPermission {Read,Update}` in `PermissionCatalog`, service/repository/DTOs, `AppSettingsSeeder`, `GET/PUT api/admin/app-settings`, `admin/settings` Angular page) — mirrors the multi-tenant cores' `MultiTenantSettings` pattern, which hekucoreapp lacked. When the toggle is **off** (default) accounts are created `EmailConfirmed = true` and nothing changes; when **on**, `AuthService.RegisterAsync` creates the user unconfirmed, emails a `/confirm-email?userId=&token=` link (new `EmailTemplates.ConfirmEmail`, `App:FrontendBaseUrl` config with request-origin fallback), issues no token, and `LoginAsync` hard-blocks unconfirmed users (after the password check, so it's not an email oracle). New `POST api/auth/confirm-email` + `POST api/auth/resend-confirmation` (always 200). Google OAuth is unaffected and now also confirms a pre-existing unconfirmed account on link. Admin-provisioned users (`UserManagementRepository`) are created confirmed. Migration `AddAppSettings` creates the table and **grandfathers every existing account** (`UPDATE "AspNetUsers" SET email_confirmed = true`), so turning the toggle on later only gates new registrations. Frontend: `confirm-email` dual-mode page (auto-confirm from link params / "check your inbox" + resend), `register` redirects there, `login` surfaces a resend link on failure, i18n en+es. **Verified:** `dotnet build` + `ng build` clean; migration created and `has-pending-model-changes` reports none. **Not browser-verified** — hekucoreapp's Supabase DB is unreachable (project reaped). Ported to ludemia, gestamind and hekutenantcoreapp the same day (gestamind's migration applied to its live DB).
- [x] **Moved `BootstrapAdminEmail` out of `appsettings.json` into user-secrets (2026-09-09)** — the key in `backend/Hekucoreapp.Api/appsettings.json` held a real personal email in a tracked, non-gitignored file. Set it via `dotnet user-secrets set "BootstrapAdminEmail" "<email>" --project backend/Hekucoreapp.Api` and blanked the default to `""` in `appsettings.json` (same pattern already used for `Jwt:Key`, `SmtpUser`, `SmtpPassword`). `Program.cs` already skips break-glass admin recovery when the value is empty (`if (!string.IsNullOrEmpty(bootstrapEmail))`), so a blank default is safe. The address stays in git history — this only stops further exposure. Fixed in gestamind and hekutenantcoreapp in the same sweep; ludemia already had it (originated there).
- [x] **Renamed `backend/Hekucoreapp.Infrastructure/Respositories/` → `Repositories/` (2026-09-09)** — the folder name was misspelled while the namespace declared inside the files already read `Hekucoreapp.Infrastructure.Repositories`. Pure `git mv` folder rename, no code change (SDK-style csproj globs `.cs`); backend build clean afterward. Fixed in gestamind and hekutenantcoreapp in the same sweep; ludemia already had it (originated there).
